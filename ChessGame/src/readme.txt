game file has the main method that runs a gameboard(chess)
the gameboard creates a array of tiles and each tile has a button
The boardlistener class has reference to the gameboard and is applied on every
button. The gameboard setups up the inital chess pieces and the a action listener is used
to move the pieces and call the update function to update the board after each move.
